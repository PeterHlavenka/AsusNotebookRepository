<Window x:Class="TechnikaViceViewProJedenViewModel.ProgramEditorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:cm="http://caliburnmicro.com"
        xmlns:cal="http://www.caliburnproject.org"
        xmlns:local="clr-namespace:TechnikaViceViewProJedenViewModel"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance local:ProgramEditorViewModel}"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        
        <ComboBox Margin="45, 5" ItemsSource="{Binding ComboItemsSource}" SelectedItem="{Binding ViewByEnvironment, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"></ComboBox>
        
        <ContentControl Grid.Row="1"
            cal:View.Context="{Binding ViewByEnvironment}"
            cal:View.Model="{Binding}" />
    </Grid>

</Window>