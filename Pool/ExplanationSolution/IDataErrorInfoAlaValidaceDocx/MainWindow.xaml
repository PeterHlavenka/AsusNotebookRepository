<Window x:Class="IDataErrorInfoAlaValidaceDocx.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IDataErrorInfoAlaValidaceDocx"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance local:MainWindowViewModel}"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <TextBlock HorizontalAlignment="Center">
            Validuje velke pismena, male pismeno je error, validuje na PropertyChanged. Trida ktera se validuje musim implementovat IDataErrorInfo a Notifikace.. 
        </TextBlock>
        
        <TextBox Grid.Row="1" Height="23" HorizontalAlignment="Center" Width="236"
                 Text="{Binding Text, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True, NotifyOnValidationError=True}" ToolTip="{Binding Path=Error}">
        </TextBox>

    </Grid>
</Window>
