<UserControl x:Class="WpfUniverse.Views.GalaxyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfUniverse.Views"
             xmlns:vm="clr-namespace:WpfUniverse.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="4*"/>
        </Grid.ColumnDefinitions>

        <!--Galaxie-->
        <TextBlock Grid.Row="0" Margin="5" FontWeight="Bold" Grid.ColumnSpan="2">Galaxie</TextBlock >
        <DataGrid Grid.Row="1" Grid.Column="1" Margin="5"  ItemsSource="{Binding ListOfGalaxies }" SelectedIndex="0"   SelectionMode="Extended" SelectedItem="{Binding SelectedGalaxy}" ></DataGrid>


        <StackPanel Margin="20,35,5,5" Grid.Column="0" Grid.Row="1">
            <Button Height="30" Margin="0,0,0,10" Command="{Binding EditGalaxy}">Editovat Galaxii</Button>
        </StackPanel>

    </Grid>
</UserControl>
