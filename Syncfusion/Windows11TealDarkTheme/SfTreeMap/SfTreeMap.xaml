<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:system="clr-namespace:System;assembly=mscorlib"
                    xmlns:themelocal="clr-namespace:Syncfusion.Themes.Windows11TealDarkTheme.WPF"
                    xmlns:sfskin="clr-namespace:Syncfusion.SfSkinManager;assembly=Syncfusion.SfSkinManager.WPF"
                    xmlns:treeMap="clr-namespace:Syncfusion.UI.Xaml.TreeMap;assembly=Syncfusion.SfTreeMap.WPF">

    <ResourceDictionary.MergedDictionaries>
        <sfskin:ResourceDictionaryProvider DictionaryName="Common" ThemeName="Windows11TealDarkTheme"/>
        <sfskin:ResourceDictionaryProvider DictionaryName="Brushes" ThemeName="Windows11TealDarkTheme"/>
    </ResourceDictionary.MergedDictionaries>

    <treeMap:VisibilityConverter x:Key="VisibilityConv" />

    <Style x:Key="SyncfusionSfTreeMapItemStyle"
           TargetType="treeMap:TreeMapItem">
        <Setter Property="BorderBrush"
                Value="Transparent" />
        <Setter Property="Background"
                Value="Transparent" />
        <Setter Property="IsTabStop" Value="False"/>
         <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="treeMap:TreeMapItem">
                    <Border Name="PART_TreeMapItemBorder" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                        <Grid Name="PART_TreeMapItemGrid" Background="{TemplateBinding Background}">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="SyncfusionSfTreeMapLegendStyle"
           TargetType="treeMap:TreeMapLegend">
        <Setter Property="Foreground"
                Value="{DynamicResource {sfskin:ThemeKey Theme={x:Type themelocal:Windows11TealDarkThemeSkinHelper}, Key=PrimaryForeground}}" />
        <Setter Property="FontFamily"
                Value="{DynamicResource {sfskin:ThemeKey Theme={x:Type themelocal:Windows11TealDarkThemeSkinHelper}, Key=ThemeFontFamily}}" />
        <Setter Property="FontSize"
                Value="{DynamicResource {sfskin:ThemeKey Theme={x:Type themelocal:Windows11TealDarkThemeSkinHelper}, Key=CaptionText}}" />
        <Setter Property="FontWeight"
                Value="{StaticResource Windows11TealDarkTheme.FontWeightNormal}" />
        <Setter Property="BorderBrush"
                Value="{StaticResource BorderAlt}" />
        <Setter Property="IsTabStop" Value="False"/>
         <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="treeMap:TreeMapLegend">
                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" 
                            Padding="{TemplateBinding Padding}" Margin="{TemplateBinding Margin}">
                        <StackPanel Orientation="Vertical">
                            <ContentPresenter Name="PART_LegendHeader" TextBlock.Foreground="{StaticResource ContentForeground}" Content="{Binding Header, RelativeSource={RelativeSource TemplatedParent}}" VerticalAlignment="{TemplateBinding VerticalAlignment}" HorizontalAlignment="{TemplateBinding HorizontalAlignment}"/>
                            <ItemsPresenter Name="PART_LegendItems" VerticalAlignment="{TemplateBinding VerticalAlignment}" HorizontalAlignment="{TemplateBinding HorizontalAlignment}"/>
                        </StackPanel>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="ItemTemplate">
            <Setter.Value>
                <DataTemplate DataType="treeMap:TreeMapLegendItem">
                    <StackPanel Orientation="Horizontal" Margin="{Binding ItemMargin}">
                        <ContentPresenter ContentTemplate="{Binding IconTemplate}"/>
                        <TextBlock Margin="{Binding ElementMargin}" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=treeMap:TreeMapLegend}}" Text="{Binding Label}" VerticalAlignment="Center"/>
                    </StackPanel>
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="ItemsPanel">
            <Setter.Value>
                <ItemsPanelTemplate>
                    <treeMap:TreeMapLegendPanel/>
                </ItemsPanelTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="SyncfusionSfTreeMapStyle"
           TargetType="treeMap:SfTreeMap">
        <Setter Property="Foreground"
                Value="{DynamicResource {sfskin:ThemeKey Theme={x:Type themelocal:Windows11TealDarkThemeSkinHelper}, Key=PrimaryForeground}}" />
        <Setter Property="TreeMapResourceDictionary">
            <Setter.Value>
                <ResourceDictionary Source="SfTreeMap.xaml" />
            </Setter.Value>
        </Setter>
        <Setter Property="LeafColorMapping">
            <Setter.Value>
                <treeMap:RangeBrushColorMapping>
                    <treeMap:RangeBrushColorMapping.Brushes>
                        <treeMap:RangeBrush From="0"
                                            To="1"
                                            LegendLabel="1 % Growth"
                                            Color="{StaticResource Series1.Color}"/>
                        <treeMap:RangeBrush From="0"
                                            To="2"
                                            LegendLabel="2 % Growth"
                                            Color="{StaticResource Series5.Color}" />
                        <treeMap:RangeBrush From="0"
                                            To="3"
                                            LegendLabel="3 % Growth"
                                            Color="{StaticResource Series2.Color}" />
                        <treeMap:RangeBrush From="0"
                                            To="4"
                                            LegendLabel="4 % Growth"
                                            Color="{StaticResource Series4.Color}" />
                    </treeMap:RangeBrushColorMapping.Brushes>
                </treeMap:RangeBrushColorMapping>
            </Setter.Value>
        </Setter>
        <Setter Property="Background"
                Value="Transparent" />
        <Setter Property="BorderBrush"
                Value="{StaticResource BorderAlt}" />
        <Setter Property="BorderThickness"
                Value="0" />
        <Setter Property="FontSize"
                Value="{DynamicResource {sfskin:ThemeKey Theme={x:Type themelocal:Windows11TealDarkThemeSkinHelper}, Key=CaptionText}}" />
        <Setter Property="FontWeight"
                Value="{StaticResource Windows11TealDarkTheme.FontWeightNormal}" />
        <Setter Property="FontFamily"
                Value="{DynamicResource {sfskin:ThemeKey Theme={x:Type themelocal:Windows11TealDarkThemeSkinHelper}, Key=ThemeFontFamily}}" />
        <Setter Property="HighlightHoverBrush"
                Value="{StaticResource BorderAlt1}" />
        <Setter Property="HighlightBorderBrush"
                Value="{StaticResource BorderAlt4}" />
        <Setter Property="DrillDownSelectionStroke"
                Value="{StaticResource Series11}" />
        <Setter Property="DrillDownHeaderTemplate">
            <Setter.Value>
                <DataTemplate>
                    <Border Background="{StaticResource ContentBackgroundAlt2}">
                        <StackPanel Orientation="Horizontal"
                                    VerticalAlignment="Center"
                                    Margin="10 0">
                            <Path x:Name="path"
                                  Height="16"
                                  Width="8"
                                  Stroke="Transparent"
                                  Fill="{StaticResource IconColor}"
                                  Stretch="Fill">
                                <Path.Data>
                                    <PathGeometry>M197,153.5 L197,138 186.75,145.5 z</PathGeometry>
                                </Path.Data>
                            </Path>
                            <TextBlock Text="{Binding}"
                                       Margin="10 0"
                                       FontSize="15"
                                       FontWeight="Medium"
                                       FontFamily="Segoe UI"
                                       Foreground="{StaticResource ContentForeground}" />
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="ToolTipTemplate">
            <Setter.Value>
                <DataTemplate>
                    <Border IsHitTestVisible="False"
                            Margin="-6,-4,-6,-4"
                            CornerRadius="{StaticResource Windows11TealDarkTheme.CornerRadius4}"
                            Background="{StaticResource TooltipBackground}"
                            Width="122"
                            BorderBrush="{StaticResource TooltipBorder}"
                            BorderThickness="{StaticResource Windows11TealDarkTheme.BorderThickness1}">
                        <StackPanel HorizontalAlignment="Left"
                                    Margin="6,4.5,6,4.5">
                            <TextBlock Text="{Binding Label}"
                                       Foreground="{StaticResource TooltipForeground}"
                                       FontWeight="Light"
                                       FontSize="{DynamicResource {sfskin:ThemeKey Theme={x:Type themelocal:Windows11TealDarkThemeSkinHelper}, Key=CaptionText}}"
                                       HorizontalAlignment="Left"
                                       VerticalAlignment="Center"
                                       Visibility="{Binding TreeMap.LeafLabelPath, Converter={StaticResource VisibilityConv}}" />
                            <StackPanel VerticalAlignment="Center">
                                <StackPanel Orientation="Horizontal"
                                            VerticalAlignment="Center"
                                            Visibility="{Binding TreeMap.WeightValuePath, Converter={StaticResource VisibilityConv}}">
                                    <TextBlock Text="{Binding TreeMap.WeightValuePath}"
                                               Foreground="{StaticResource TooltipForeground}"
                                               FontWeight="Normal"
                                               FontSize="{DynamicResource {sfskin:ThemeKey Theme={x:Type themelocal:Windows11TealDarkThemeSkinHelper}, Key=CaptionText}}" />
                                    <TextBlock Text=" - "
                                               Foreground="{StaticResource TooltipForeground}"
                                               FontWeight="Normal"
                                               FontSize="{DynamicResource {sfskin:ThemeKey Theme={x:Type themelocal:Windows11TealDarkThemeSkinHelper}, Key=CaptionText}}" />
                                    <TextBlock Text="{Binding Weight}"
                                               Foreground="{StaticResource TooltipForeground}"
                                               FontWeight="Normal"
                                               FontSize="{DynamicResource {sfskin:ThemeKey Theme={x:Type themelocal:Windows11TealDarkThemeSkinHelper}, Key=CaptionText}}" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal"
                                            VerticalAlignment="Center"
                                            Visibility="{Binding TreeMap.ColorValuePath, Converter={StaticResource VisibilityConv}}">
                                    <TextBlock Text="{Binding TreeMap.ColorValuePath}"
                                               Foreground="{StaticResource TooltipForeground}"
                                               FontWeight="Normal"
                                               FontSize="{DynamicResource {sfskin:ThemeKey Theme={x:Type themelocal:Windows11TealDarkThemeSkinHelper}, Key=CaptionText}}" />
                                    <TextBlock Text=" - "
                                               Foreground="{StaticResource TooltipForeground}"
                                               FontWeight="Normal"
                                               FontSize="{DynamicResource {sfskin:ThemeKey Theme={x:Type themelocal:Windows11TealDarkThemeSkinHelper}, Key=CaptionText}}" />
                                    <TextBlock Text="{Binding ColorWeight}"
                                               Foreground="{StaticResource TooltipForeground}"
                                               FontWeight="Normal"
                                               FontSize="{DynamicResource {sfskin:ThemeKey Theme={x:Type themelocal:Windows11TealDarkThemeSkinHelper}, Key=CaptionText}}" />
                                </StackPanel>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="treeMap:SfTreeMap">
                    <Grid Name="PART_TreeMap"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <DataTemplate x:Key="FlatLevelDefaultHeaderTemplate">
        <ContentPresenter>
            <ContentPresenter.Content>
                <TextBlock Text="{Binding Header}"
                           FontWeight="Medium"
                           FontSize="12"
                           Foreground="{StaticResource ContentForeground}"
                           Margin="0,8,0,0"
                           Padding="8,6,0,6"
                           Background="{StaticResource ContentBackgroundAlt2}"
                           HorizontalAlignment="Stretch"
                           VerticalAlignment="Center" />
            </ContentPresenter.Content>
        </ContentPresenter>
    </DataTemplate>

    <Style BasedOn="{StaticResource SyncfusionSfTreeMapItemStyle}"           TargetType="treeMap:TreeMapItem" />
    <Style x:Key="{sfskin:DefaultStyleKeyExtension Theme={x:Type themelocal:Windows11TealDarkThemeSkinHelper}, Control=Syncfusion.UI.Xaml.TreeMap.TreeMapItem}" BasedOn="{StaticResource SyncfusionSfTreeMapItemStyle}"           TargetType="treeMap:TreeMapItem" />

    <Style BasedOn="{StaticResource SyncfusionSfTreeMapLegendStyle}"           TargetType="treeMap:TreeMapLegend" />
    <Style x:Key="{sfskin:DefaultStyleKeyExtension Theme={x:Type themelocal:Windows11TealDarkThemeSkinHelper}, Control=Syncfusion.UI.Xaml.TreeMap.TreeMapLegend}" BasedOn="{StaticResource SyncfusionSfTreeMapLegendStyle}"           TargetType="treeMap:TreeMapLegend" />
    
    <Style BasedOn="{StaticResource SyncfusionSfTreeMapStyle}"           TargetType="treeMap:SfTreeMap" />
    <Style x:Key="{sfskin:DefaultStyleKeyExtension Theme={x:Type themelocal:Windows11TealDarkThemeSkinHelper}, Control=Syncfusion.UI.Xaml.TreeMap.SfTreeMap}" BasedOn="{StaticResource SyncfusionSfTreeMapStyle}"           TargetType="treeMap:SfTreeMap" />
    
</ResourceDictionary>
