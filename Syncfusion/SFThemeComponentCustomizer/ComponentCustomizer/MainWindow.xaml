<Window x:Class="ComponentCustomizer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="MainWindow" Height="800" Width="1000">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <ComboBox Grid.Column="0"
                  x:Name="ThemeSelector"
                  Height="40"
                  Width="350"
                  Margin="10"
                  SelectedIndex="1"
                  SelectionChanged="Theme_OnSelectionChanged" />
        <ComboBox Grid.Row="0" Grid.Column="1"
                  x:Name="PrimaryBackgroundSelector"
                  Height="40"
                  Width="350"
                  Margin="10"
                  SelectedIndex="1" 
                  SelectionChanged="Primary_OnSelectionChanged" />
        <ComboBox Grid.Row="0" Grid.Column="2"
                  x:Name="ComponentSelector"
                  Height="40"
                  Width="350"
                  Margin="10"
                  SelectionChanged="Component_OnSelectionChanged" />
        <ContentControl x:Name="ContentControl" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3" Margin="10" />
    </Grid>
</Window>