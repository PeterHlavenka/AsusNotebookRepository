<UserControl x:Class="WpfUniverse.Gui.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:views="clr-namespace:WpfUniverse.Gui.Views"
             mc:Ignorable="d" 
             Width="1500"
             d:DesignHeight="900" d:DesignWidth="2000">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="8*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>



        <!--USER CONTROLS-->
        <views:GalaxyView DataContext="{Binding Path=GalaxyViewModel}" />
        <views:PlanetView Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" DataContext="{Binding Path=PlanetsViewModel}" />
    </Grid>
</UserControl>
